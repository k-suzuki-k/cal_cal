.row
  .col-sm-6
    h2.post_new_title 編集ページ
    = form_with model: @post, url: post_path, class: "validate_field", id: "new_post" do |form|

      .form-group
        = form.label :day, '日付'
        = form.date_field :day, class: 'form-control', id: 'todo_day';

      .form-group
        = form.label :title, 'タイトル'
        = form.text_field :title, class: 'form-control'

      .form-group
        = form.label :content, 'コメント'
        = form.text_area :content, class: 'form-control', rows:2

      = form.submit '編集する', class: 'post_submit btn btn-primary mr-4 mb-4'

    = link_to "キャンセル", posts_path, class: 'btn btn-warning'

  .col-sm-6
    table#todo_lists.table
      h3.table_title
      tbody#todo_search_result
